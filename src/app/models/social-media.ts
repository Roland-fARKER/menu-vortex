export interface SocialMedia {
    id: string
    name: string
    url: string
    icon: string
  }
  
  export interface MapLocation {
    lat: number
    lng: number
    zoom: number
    title: string
    address: string
  }
  